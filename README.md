# nuxt-blog

Work in progress blog frontend built with Nuxt, made to sit on top of a Laravel API backend. Public blog and post pages, plus an admin area behind auth for writing and managing posts.

```
npm install
npm run dev
```
